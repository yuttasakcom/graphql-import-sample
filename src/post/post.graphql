type Query {
  posts: [Post!]!
}

type Mutation {
  createPost(data: CreatePostInput!): Post!
}

type Subscription {
  postCreated: Post!
}

type Post {
  title: String!
  body: String!
}

input CreatePostInput {
  title: String!
  body: String!
}
