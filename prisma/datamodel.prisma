type User {
  id: ID! @id
  name: String!
  posts: [Post!]! @relation(link: INLINE, name: "PostToUser", onDelete: CASCADE)
}

type Post {
  id: ID! @id
  title: String!
  body: String!
  author: User!
    @relation(link: INLINE, name: "AuthorToUser", onDelete: SET_NULL)
}
